# 下一阶段开发计划

更新日期：2026-08-29
规划范围：`v0.1.2` 已发布稳定基线与 `v0.2.0` 质量阶段

本计划的 Bug 修复与 UI 打磨执行清单见 [Bug 修复与 UI 打磨开发计划](bug-ui-polish-plan.md)。
该清单把本文件的阶段 B、阶段 C 和阶段 C+ 拆成可逐项验收的开发切片；范围发生变化时，先更新本文件，
再更新执行清单。

## 1. 当前基线

Flash Shot 的功能主线已经完成。Windows 单显示器、100% 缩放环境中的截图、标注、复制、
保存、Pin、历史、滚动截图、OCR 和录屏闭环已有 Release 原生验收；CI、便携包、安装器、
校验清单与 GitHub draft Release 流程也已建立。

下一阶段不再以增加截图工具或在线服务数量为目标，而是把现有能力变成可持续维护、可重复发布、
可在更多真实 Windows 环境中证明可靠的产品。

截至本计划更新时：

- `v0.1.2` 已于 2026-08-16 从提交 `d17cfe9` 发布为 GitHub Latest Release；标签、Cargo、安装器、
  便携包、sidecar 和 manifest 使用同一版本事实；
- `main` 保持 `0.1.2` 应用版本，并包含发布说明生成器的后续修复；GitHub 上的 `v0.1.1` 仍是历史
  draft，不再是候选版本；
- 单显示器 100% 主流程已通过，150%/200% 缩放仍缺少真实硬件证据；
- 负坐标双屏与混合 DPI 双屏仍按既有决定暂缓，不纳入当前阶段承诺；
- 配置真实 HTTPS 端点后的翻译成功、失败与重试闭环尚未执行，已与双屏验收一起移出当前阶段；
- 4K 标注的真实交互帧时间尚未形成可重复的目标硬件基线；
- 项目已拆成领域、图像、Windows 基础设施、应用与唯一二进制五个 workspace crate；
  `flash-shot-app::platform` 只保留兼容导出；当前列出的 Windows 基础设施迁移切片已经完成，但仍嵌在
  应用用例中的系统集成契约、`overlay.rs` 与原生验收 runner 需要继续按职责拆分。
- 应用已建立独立于 OCR 翻译服务的 `Locale`/`UiText` 本地化基础，首次启动跟随 Windows 首选 UI 语言，
  设置导航、捕获覆盖层、标注工具、Library 历史管理和 Pin 工具栏已覆盖 English 与简体中文；OCR/二维码/翻译工作流
  与 Capture 设置中的支持检查已完成首批迁移，Record 的设置、生命周期、目录、来源和支持状态也已完成一组迁移；
  更新检查、快速保存目录、历史保留、历史选择/清理/删除、选区 Save/Quick Save 以及标注项目 Save/Open 状态已完成分组迁移，
  Pin/全屏导出、选区/全屏复制结果、历史复制、通用剪贴板忙态、Capture 偏好标签/快捷键摘要/延迟/光标/颜色/导出/设置窗口反馈
  以及滚动截图工作流和控制器状态、标注工具选择/绘制/编辑反馈、标注上下文操作反馈、捕获生命周期与编辑过程反馈、图片/项目打开、快捷键/自启动/外观、窗口和 Pin 操作反馈已完成分组迁移，其余动态状态文案仍需继续迁移。
- 托盘已把区域截图恢复为首个直接动作，覆盖层用稳定的主操作与 `More` 菜单区分高低频功能；
  Library、Record、诊断和错误状态仍需继续收敛信息层级。

详细环境状态与证据以 [Windows 手工验收记录](windows-manual-acceptance.md) 为准。本计划只保留
下一阶段的目标、顺序和退出标准，不再复制逐次运行日志。

## 2. 开发原则

1. **核心路径与界面优先**：当前阶段不增加新的截图、标注、OCR、翻译或录屏能力；先保证截图主链稳定，
   再完成视觉一致性与国际化。OCR 是不阻塞截图的可选能力，真实翻译服务和双屏验收不在当前范围。
2. **发布基于当前主线**：不得直接发布早于生产修复的旧 draft 资产；标签、源码、runner 和验收报告
   必须指向同一提交。
3. **一个风险一个切片**：行为修复、结构重构、依赖升级和文档变更分别提交，验证通过后立即推送。
4. **真实证据与静态门禁并存**：原生窗口、全局输入、系统剪贴板、FFmpeg 和 DPI 行为不能只靠单元测试
   宣称通过。
5. **失败必须可恢复**：超时、前台抢占、剪贴板争用、磁盘错误和外部进程异常都必须安全停止，保留
   可诊断结果，并清理输入、窗口、临时文件与子进程。
6. **控制文档寿命**：需求、当前计划、操作流程和验收矩阵各保留一个事实来源；已完成的过程计划留在
   Git 历史，不继续在 `docs` 中累积。

## 3. 优先级

| 优先级 | 含义 | 当前主题 |
| --- | --- | --- |
| P0 | 阻塞可信发布或可能破坏用户数据/主流程 | 当前主线补丁发布、复制/保存/录屏清理、发布资产一致性 |
| P1 | 不阻塞补丁发布，但影响长期稳定与开发效率 | 故障注入、长时间资源门禁、大文件拆分、4K 交互基线、UI 视觉统一与国际化 |
| P2 | 依赖额外硬件、服务或明确产品决策 | 150%/200% 矩阵、双屏混合 DPI、真实翻译端点、Linux/macOS |

P0 未关闭前不开始新功能。P1 重构不得与行为变化混在同一提交。P2 只在前置环境可用或范围被明确恢复
后执行。

### 当前阶段范围（已确认）

当前阶段以可用、清晰的核心截图体验为唯一产品主线：

- **纳入交付**：全局快捷键、选区、标注、撤销/重做、复制、保存、Pin 与其错误恢复保持稳定；统一
  Capture、Library、Record、App 和 Pin 的信息层级、视觉 token 与中英文文案。
- **可选维护**：本地 OCR 在依赖可用时提供识别、复制和重试；依赖缺失或识别失败不得阻塞截图、标注、
  复制或保存。
- **暂不交付**：真实 HTTPS 翻译服务的成功、失败和重试闭环；负坐标双屏与混合 DPI 双屏验收。它们保留
  在 P2，只有产品范围明确恢复后才重新排期。

## 4. 阶段 A：发布当前主线补丁版（已完成）

结果：`v0.1.1` 标签之后已经验证的修复已收敛为 `v0.1.2`，没有发布不包含最新剪贴板修复的旧
draft。

### A1. 冻结与版本边界（已完成）

- 已审计 `v0.1.1..main` 的提交范围，包含应进入补丁版的修复、本地化、结构重构、测试、CI 与文档变更；
- Cargo 版本已提升到 `0.1.2`，锁文件、安装器、脚本夹具和用户文档使用同一版本事实；
- Release 工作流从标签提交范围生成双语 changelog，不使用 GitHub 默认说明；
- 既有 `v0.1.1` 标签保持不变，旧 draft 不作为当前发布候选。

### A2. 当前源码发布门禁（已完成）

必须在同一候选提交上完成：

```powershell
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

然后执行与本次补丁直接相关的 Release 验收：

- 工具栏 Copy 与 Enter Copy 各 1 次预热和 30 次有效系统剪贴板样本；
- 三 Pin 共存、生产剪贴板复制、Capture/Cancel、逐个关闭与 60 秒生命周期 soak；
- 普通截图的 Save、Pin、Copy 和 teardown 清理闭环；
- 显示器、区域、窗口录制的开始、暂停、恢复、停止、MP4/FFprobe 与子进程清理；
- 便携包启动和安装、启动、卸载生命周期；
- release manifest、SHA-256 sidecar、资产清单和版本一致性。

系统剪贴板与全局输入场景只在可丢弃桌面显式授权运行。报告、截图、runner 哈希和构建提交必须能相互
对应。

`d17cfe9` 候选提交已通过格式、全 workspace 编译、严格 Clippy、600 项 workspace 测试、Release 脚本
fixture、单屏 100% 的生产剪贴板批次、Pin 生命周期、保存/复制与录屏证据。发布工作流随后在 fresh
Windows runner 上再次执行源码门禁、Release 构建、便携启动和安装器安装/启动/卸载生命周期。

### A3. 发布退出标准（已完成）

- 新标签从通过全部门禁的提交创建；
- GitHub hosted runner 成功生成新的 draft 资产；
- 下载后的安装器、便携包、sidecar 和 manifest 全部复核通过；
- 人工完成核心截图、标注、复制、保存、Pin 与录屏冒烟后再发布；
- 旧 `v0.1.1` draft 不再被描述为当前候选版本。

GitHub Release workflow `31950151868` 已成功生成并验证五项发布资产；本机在 draft 阶段和公开发布后
分别下载复核安装器、便携包、sidecar 与 manifest。`v0.1.2` 于 `2026-08-16T14:00:39Z` 发布为
[Latest Release](https://github.com/bruceblink/flash-shot/releases/tag/v0.1.2)。完整资产摘要和生命周期证据见
[Windows 手工验收记录](windows-manual-acceptance.md)。

## 5. 阶段 B：可靠性与故障恢复

目标：从“功能能用”进入“故障发生时仍可预测”。每一项都是独立的小功能或测试切片。

### B1. 主流程故障注入

- 剪贴板被短暂占用、消费者超时和 owner HWND 创建失败时，验证有界重试、错误反馈与零残留；
- 快速保存目录只读、临时文件同步失败、目标名冲突和历史索引写入失败时，验证原子性与回退路径；
- FFmpeg 缺失、启动失败、运行中退出和停止超时时，验证状态恢复、MP4 处理和 Job Object 清理；
- 历史目录包含损坏 PNG、文件消失或切换目录时，验证有界队列继续处理且不会写回陈旧结果。

退出标准：每个故障都由确定性测试或隔离探针复现；失败后应用可立即重新截图或录屏，且没有孤儿进程、
窗口、按键、半成品文件或永久 busy 状态。

### B2. 长时间资源稳定性

- 先记录 Capture/Copy/Pin/Close、历史浏览和录屏停止的 Release 长时基线，再根据实测波动设门槛；
- 报告句柄、线程、工作集、私有提交、窗口注册表、后台任务和子进程的起点、峰值与结束值；
- 将短时波动与持续增长分开，禁止凭单次样本定义通用内存阈值。

退出标准：重复操作后资源回到稳定区间，Capture preflight 可用，报告可由 CI 外的受控 Windows 会话
重复执行。

### B3. 性能基线

- 保持启动、快捷键到覆盖层、Copy 外部消费者和连续捕获现有门槛；
- 建立 4K 非空标注的真实交互帧时间基线，区分 GPUI 预览与 CPU 导出；
- 为长图 PNG 编码记录尺寸、吞吐、峰值内存和像素指纹，不把文件对话框等待混入编码指标。

退出标准：每项指标有清楚的计时边界、预热、样本数、p50、p95、最大值和失败数；回归门禁来自稳定
基线而不是任意百分比。

## 6. 阶段 C：可维护性改造

目标：降低原生交互代码和验收工具继续演进的成本，不改变用户行为。

### C1. 拆分原生验收 runner

- 将 `overlay-interaction-acceptance` 按 Capture、Copy、Pin、Scroll、Recording 和共享 Windows 输入设施
  拆分模块；
- 保持现有 CLI 参数、默认安全策略、报告 schema 与输出目录兼容；
- 抽取前台所有权、按键释放、watchdog、截图和 cleanup 断言，避免场景各自实现不同规则。

每次只迁移一个场景，迁移前后用同一场景的单元测试与 Release 报告比较；禁止一次性重写整个 runner。

### C2. 收敛应用模块边界

- 按渲染、输入、选择变换、菜单/工具状态和导出生命周期逐步缩小 `crates/flash-shot-app/src/app/overlay.rs`；
- 继续把文件 I/O、识别、录屏和平台调用留在现有 workflow/platform 边界；
- 为非平凡状态更新和 Windows 生命周期函数补充面向初学者的函数级注释；
- 模块边界已通过多个切片验证后，采用小型 workspace；领域、应用、Windows 基础设施、GPUI 界面、验收库
   和唯一二进制的职责与迁移顺序见 [Workspace crate 迁移设计](workspace-crate-design.md)。第一阶段已完成
   `flash-shot-domain`、`flash-shot-app` 与 `crates/flash-shot-bin` 中唯一 `flash-shot` 包的工作区落地，
   第二阶段已完成 `flash-shot-image` 的图像核心提取，Windows 基础设施已完成显示器枚举、屏幕捕获、
   全局快捷键、托盘、剪贴板、自启动、目录打开、进程生命周期、窗口控制、光标定位和辅助滚轮输入切片；
   `flash-shot-app::platform` 现只保留兼容导出，但整体基础设施阶段仍需继续隔离应用用例中的系统集成契约。

### C3. 依赖与供应链维护

- 以独立提交审计 Rust 依赖公告、许可证和未使用依赖；
- GPUI/Zed 升级单独立项，必须通过编译、真实覆盖层、设置、Pin、性能和打包矩阵；
- 不在功能修复中顺带升级锁定的 GPUI 提交或 FFmpeg 契约。

退出标准：行为和报告契约不变，全量门禁通过，大文件的职责边界与测试归属更清楚。

## 6.5 阶段 C+：UI 信息架构、视觉系统与国际化

目标：在不改变截图、标注、录屏和历史数据契约的前提下，重做用户界面层级、视觉语言和文案资源，
让核心操作更短、更容易理解，也让后续能力可以按模块扩展。

### C+1. 信息架构与动作收敛

当前进度：托盘的区域截图已成为首个直接动作，捕获覆盖层已把 Copy、Save、Pin、Cancel 保持为稳定
主操作，并把滚动截图、识别和录屏等低频命令收进 `More` 菜单。后续继续处理设置页与 Library/Record
页面中的重复入口和诊断层级。

- 将 `Capture` 作为唯一的高频主入口，保留区域、全屏和焦点窗口三种核心捕获动作；
- 从设置页移除重复的“全屏 Copy/Save/Pin”快捷按钮，统一在覆盖层、托盘或明确的 `More` 菜单中提供；
- 将低频诊断、目录检查、更新检查和兼容性操作放到对应设置分组的次级区域，不与主动作并排竞争；
- 保持核心流程最多两层可达，按钮名称描述结果而不是内部实现；
- 为每个保留动作定义稳定的语义 ID，避免通过显示文案判断行为。

退出标准：Capture、Library、Record、App 四个入口的职责不重叠；真实键鼠操作可以从设置或覆盖层完成
截图、标注、Copy、Save、Pin、录屏和清理，不需要寻找重复按钮。

### C+2. 应用级国际化基础

- 已新增独立于 OCR/外部翻译服务的 UI `Locale` 与稳定 `UiText` 资源目录；
- 已支持 English 与简体中文，首次启动跟随 Windows 首选 UI 语言，并保留英文安全回退；
- 语言选择已写入版本化用户设置，启动时加载，切换后刷新当前 GPUI surface；
- 设置导航、捕获主操作、更多操作、标注工具、Library 历史管理和 Pin 工具栏已迁移；OCR/二维码/翻译工作流与 Capture
  设置中的支持检查已完成首批迁移，Record 的设置、生命周期、目录、来源和支持状态、更新检查、快速保存目录与历史保留状态已完成分组迁移，
  历史选择/清理/删除反馈、选区 Save/Quick Save、标注项目 Save/Open、标注工具选择/绘制/样式/文字编辑反馈、标注上下文操作反馈、捕获生命周期与编辑过程反馈、Pin/全屏导出、选区/全屏复制结果、历史复制、通用剪贴板忙态、
  Capture 设置页偏好标签/快捷键摘要/延迟/光标/颜色/导出/设置窗口反馈、滚动截图工作流/控制器状态和图片/项目打开、快捷键/自启动/外观、窗口及 Pin 操作反馈已完成分组迁移，其余动态状态继续分组迁移；
  翻译只保留配置或不可用状态的本地化文案，不验收真实服务链路；
- 禁止在用户可见 UI 中继续拼接英文状态句；动态数量、路径、快捷键和错误详情使用带参数的本地化模板；
- 资源键、按钮 ID、自动化报告字段和快捷键契约保持语言无关。

### C+3. 现代化视觉系统

- UI 重设计先执行独立的 U0 设计基线切片：先冻结表面层级、语义颜色、间距、字号、圆角、焦点态和状态反馈，
  再分组修改页面，避免每个页面各自选择颜色和按钮样式；
- 视觉方向定位为桌面创作工具：深石墨中性表面、电光青主色、绿色/琥珀/红色状态色和精确的细边界，
  禁止用霓虹堆色、渐变玻璃卡片、装饰性大面板或与截图内容无关的科技装饰制造“科技感”；
- 建立统一的语义颜色、间距、字号、圆角、边框、禁用态、焦点态和状态反馈 token；
- 统一按钮层级：每个区域最多一个主要动作，次要动作使用低对比度按钮，破坏性动作单独强调；
- 在覆盖层、Pin、设置和历史中统一工具栏高度、控件命中区、键盘焦点和错误反馈，不用装饰性面板堆叠层级；
- 优先使用图标加 tooltip 表示缩放、复制、保存、关闭等熟悉动作，长文本只保留在不易误解的命令上；
- 以 420x420、520x640、980x760 三种窗口尺寸同时校验中英文、深浅主题和窄选区布局。

### C+4. 分组迁移与验收

按以下顺序迁移，每组单独提交和验收：

1. 设置窗口导航、页标题与语言切换基础已完成；页内细节和动态系统状态继续迁移；
2. Capture 覆盖层、More 菜单、Copy/Save/Pin/Cancel 与标注工具第一批已完成；
3. Library、历史筛选、批量删除、空状态、条目动作和数量/时间状态已迁移；已在单屏 100% 的原生简中浅色
   520x640/980x760 与深色 520x640 窗口完成视觉复核，继续补充有历史条目的真实中英文视觉验收；
4. OCR/二维码/翻译工作流与 Capture 设置中的支持/测试状态已完成首批迁移；Record 的设置、生命周期、目录、来源和支持状态、更新检查、
   快速保存目录、历史保留、历史选择/清理/删除反馈、选区 Save/Quick Save、标注项目 Save/Open、Pin/全屏导出、选区/全屏复制结果、
   历史复制、通用剪贴板忙态、标注工具选择/绘制/样式/文字编辑反馈、标注上下文操作反馈、捕获生命周期与编辑过程反馈、Capture 设置页偏好标签/快捷键摘要/延迟/光标/颜色/导出/设置窗口反馈、滚动截图工作流/控制器状态以及图片/项目打开、快捷键/自启动/外观、窗口和 Pin 操作反馈
   已完成分组迁移；其余错误/忙状态待迁移；
   翻译仅迁移配置或不可用状态文案，不作为真实服务验收项；
5. Pin 工具栏、快捷键提示和窗口生命周期反馈的文案迁移已完成；真实中英文 Pin 窗口截图复核仍待执行。

退出标准：选择简体中文后，核心 UI 不再出现可见英文硬编码（品牌名、路径、快捷键和外部错误详情除外）；
所有保留功能在中英文下均可由真实鼠标和键盘完成，截图、报告、资源清理和快捷键行为保持不变。

## 7. 阶段 D：真实环境矩阵

该阶段依赖真实硬件，不能用逻辑缩放、合成截图或单元测试代替。

### D1. 单显示器高 DPI

在 150% 和 200% 缩放下分别验证：

- 智能窗口/控件候选、自由拖选、移动、普通/Shift/Alt 缩放和键盘微调；
- Copy、Save、Pin、滚动截图和录屏的物理像素尺寸；
- 设置窗口、窄选区、More 菜单、标注上下文和状态反馈的布局；
- 多 Pin 移动、缩放、透明度、复制、保存、Capture 共存和清理。

退出标准：报告中的实际 DPI/scale 与目标一致，导出逐像素正确，关键截图无截断、重叠或不可达控件。

### D2. 暂缓项

- 负坐标双屏和混合 DPI 双屏继续暂缓；恢复范围后按
  [Windows 手工验收记录](windows-manual-acceptance.md)作为独立阶段执行；
- 真实翻译端点只在产品范围恢复且有可丢弃 HTTPS 测试服务时验收；当前阶段不排期，也不作为核心发布
  阻塞项；
- Linux 和 macOS 只保留可行性研究，不承诺功能对等或发布资产。

## 8. 阶段 E：`v0.2.0` 候选版

进入候选版前必须满足：

- 阶段 A 已发布，P0 故障恢复项全部关闭；
- 至少完成一轮可重复长时资源基线和 4K 交互性能基线；
- 验收 runner 与 overlay 至少完成一个可验证的模块化切片，未引入报告契约回归；
- UI 信息架构、语言资源和核心工作流迁移至少完成一轮，中英文双主题截图与真实键鼠验收通过；
- 可用硬件范围内的 DPI 矩阵已执行，未执行项保持显式暂缓；
- README、需求、架构、计划、验收矩阵和分发文档之间没有冲突或失效链接；
- CI、Release workflow、下载复核和人工 UI 冒烟全部通过。

`v0.2.0` 仍以 Windows-first、原生 Rust/GPUI 和本地处理为产品边界。代码签名可以在具备可信证书与
运营预算后单独推进；在此之前继续清楚说明未签名资产风险，不把签名伪装成已完成。

## 9. 暂不安排

- 新的标注工具、识别引擎、翻译供应商或录屏后端；
- 协作、云同步、插件 API、浏览器版或移动版；
- 为追求形式上的功能对等而启动 Linux/macOS 全量移植；
- 未经真实 Windows 验收的 GPUI 主线跟随升级；
- 与当前稳定性、维护性或发布目标无关的大规模 UI 重设计。

## 10. 推荐执行顺序

1. `v0.1.2` 已于 2026-08-16 发布，阶段 A 关闭；
2. UI U0 设计基线已在提交 `cf8436f` 完成，形成可复用 token 和三种窗口尺寸的中英文/深浅主题基准截图；
3. 阶段 B0 已完成可复现的 Bug 与可见文案基线；阶段 B1 已完成保存失败保留原选区、历史索引失败可重试且
   中英文提示统一的确定性切片，剪贴板和 FFmpeg 失败注入仍按 B1 清单继续；
4. 随后各完成一个长时资源切片和一个 4K 性能切片；
5. 每次迁移一个验收场景，逐步拆分大型 runner；
6. 在真实硬件可用时执行 150%/200% 矩阵；
7. 汇总通过证据，进入 `v0.2.0` 候选版。

每完成一项都先运行风险匹配的验证，再以单一 Conventional Commit 提交并立即推送；未通过或只完成一部分
的项目保留在本计划中，不标记为完成。
