# Snow Shot 对齐开发计划

## 1. 目标

Flash Shot 以 Snow Shot 的高频桌面工作流为产品基线，在保留 Rust/GPUI 原生架构的
前提下，达到以下结果：

1. 截图、编辑、复制、保存、Pin 和历史复用路径稳定且符合直觉。
2. 主界面、截图覆盖层和独立工具窗口使用一致的视觉语言与交互反馈。
3. Snow Shot 中高价值、低依赖的效率能力有明确的实现或取舍。
4. Windows 实机行为、性能和资源生命周期能够被重复验证。

对齐指任务完成效率和交互清晰度，不复制 Snow Shot 的 Tauri、WebView、React、
Excalidraw 或插件实现。

## 2. 参考基线

产品基线来自 Snow Shot 官方资料：

- 官方仓库：https://github.com/mg-chao/snow-shot
- 官方功能说明：https://snowshot.top/
- 官方快速入门：https://snowshot.top/guide/

重点参考的已验证模式包括智能窗口识别、选后标注、稳定的主操作、可移动和缩放的多
Pin、截图历史、快捷入口、主题配色，以及按需加载 OCR、翻译和录屏等高级能力。

## 3. 当前状态与差距

截至 2026-08-10，Flash Shot 已实现区域与智能选区、多显示器、标注、复制与保存、
Pin、截图历史、二维码、可选 OCR/翻译、滚动截图和 FFmpeg 录屏。核心自动测试为
350 项，严格 Clippy 当前通过。

当前范围说明：用户已于 2026-08-10 明确暂缓双屏和跨屏混合 DPI 验收。相关代码与验收
命令保留，但不作为当前单屏开发的阻塞项，也不能据此标记为通过；恢复该范围时，必须重新
执行负坐标双屏和混合 DPI 双屏矩阵。

| 领域 | 当前状态 | 与 Snow Shot 的主要差距 | 优先级 |
| --- | --- | --- | --- |
| 稳定性 | 有显式截图/录屏状态、后台任务 generation、Release 资源门禁和热键测试隔离 | Windows 独立窗口的单屏运行态继续观察；真实多屏审查已暂缓 | P0 |
| 截图覆盖层 | 智能目标、自由选区、焦点窗口入口、放大镜和主次工具栏已具备；跨屏标记工具只由选区 owner 显示 | 窄选区密度和动作排序需复核；跨屏混合 DPI 实机一致性已暂缓 | P0 |
| 标注 | 主要工具、样式、图层、文字和撤销重做已具备；Undo/Redo 使用稳定位置与明确禁用态，工具选中反馈统一；已选对象将 Delete/Duplicate 与按需展开的 Arrange 分为独立上下文组 | 暂无未覆盖的 P1 工作流差距；继续保留长期交互观察 | P1（完成） |
| Pin | 多 Pin、置顶、图片优先悬停控制、自由移动/缩放、复制、保存、透明度和穿透已具备；托管 Pin 保存使用单一占用槽，重复写入会得到明确反馈并避免历史索引竞态 | 多窗口真实硬件交互和长时间生命周期仍需手工复核 | P0 |
| 历史 | 缩略图、相对时间、关键词搜索、来源筛选（含 Selection、Scrolling、Full screen 和 Pinned）、展开、单项动作、批量删除和键盘导航选择已具备；300 条真实记录默认仅渲染 5 条预览，重启截图与内存复核通过 | 暂无未覆盖的 P1 工作流差距；继续保留长期增长观察 | P1（完成） |
| 设置主页 | 分组页面、深浅主题、状态反馈和面向 Capture/Library/Record/App 的任务导航已具备；深浅主题语义前景色在背景和面板上均由自动测试保证至少 4.5:1 对比度；深色/浅色 980x760、浅色 520x640 和深浅色最小 420x420 的 Release 原生截图复核通过；设置探针现在可按零基显示器索引定位目标 DPI | 150%/200% 缩放下的最终实机复核仍需完成 | P1 |
| 保存 | PNG/JPEG/WebP 导出、快速保存、项目 sidecar、默认目录和命名配置已具备；设置页的 `Check folder` 与新目录选择都会先验证当前目录能创建、同步并清理私有探针文件；首选历史目录失效时，选择、全屏和 Pin 快速保存会回退到托管目录并同步历史索引 | 不同 Windows profile 下的目录权限和命名仍需手工复核 | P2 |
| 高级能力 | OCR、翻译、滚动截图和录屏、环境检查及失败恢复已具备；OCR 保留 `FLASH_SHOT_TESSERACT` 覆盖，同时自动识别 Windows 常见 Tesseract 安装路径；录屏停止请求现在有独立 `Stopping` 状态，收尾期间禁用重复停止/暂停/切换目标；显示器与音频源枚举有 generation 守卫，设置页在枚举期间锁定冲突录屏动作；`settings-ui-acceptance` 可确定性渲染 Starting、Recording、Paused、Stopping，并在截图前置前目标窗口以避免被桌面遮挡；设置页的 `Test service` 只在用户显式点击且配置 HTTPS 端点时发送固定短语，并仅显示返回长度，独立 generation 与忙状态不会占用 OCR/截图翻译流程；翻译边界兼容顶层 `translation`/`translatedText`、`translations[].text` 和嵌套 `data` 响应字段；`recognition-acceptance` 默认只读报告 OCR/翻译依赖，显式 `--ocr-image` 或 `--translation-text` 才会执行真实链路并仅写入长度元数据；生产录屏后端的显示器、窗口、区域与暂停/恢复/停止已由 `recording-acceptance` 验收探针生成 MP4 并通过 `ffprobe` | 翻译真实端点与 UI 成功态仍需验收，完整录屏 UI 手工矩阵仍需执行 | P2 |

## 4. 实施阶段

### 阶段 0：审查基线与计划

- [x] 建立 Snow Shot 官方功能与交互基线。
- [x] 梳理 Flash Shot 当前功能、测试和主要差距。
- [x] 规定分阶段验收与提交规则。
- [x] 建立 [Windows 手工验收记录](windows-manual-acceptance.md)，覆盖单屏、负坐标双屏和混合 DPI 双屏。

退出标准：后续每项改动都能对应到本计划中的差距和验收项。

### 阶段 1：完整代码审查与已知问题修复

1. 对截图、Pin、历史、滚动截图、录屏和应用退出的异步状态进行逐路径审查。
2. 审查 Windows HWND、剪贴板、托盘、快捷键、进程和临时文件的所有权与清理。
3. 修复 UI 线程阻塞、晚到任务覆盖新状态、窗口重入、无界缓存和错误吞没。
4. 运行格式化、严格 Clippy、全量测试和差异检查。

退出标准：

- cargo fmt --check 通过。
- cargo clippy --all-targets --all-features -- -D warnings 通过。
- cargo test 全部通过。
- 已发现的 P0/P1 正确性问题均有回归测试或明确的 Windows 验收步骤。

### 阶段 2：核心截图与标注交互

1. 保持复制、保存、Pin 和取消为稳定主动作，低频功能渐进展开。
2. 验证智能窗口单击采用、自由拖动、选区移动/缩放和键盘微调的优先级。
3. 统一标注工具的选中、悬停、禁用、样式面板和撤销重做反馈。
4. 在窄选区、屏幕边缘和跨屏选区中避免工具栏遮挡或跳动。

退出标准：区域截图从唤起到导出的完整路径无需打开设置窗口；当前单屏范围内只出现一组
可操作工具栏，像素尺寸和导出结果保持物理像素正确。多屏工具栏规则在该范围恢复后验收。

### 阶段 3：Pin、历史和快捷入口

1. Pin 采用图片优先的无边框界面，控制条仅在悬停或明确操作时出现。
2. Pin 支持自由移动、等比缩放、透明度、复制、保存、关闭和多窗口管理。
3. 历史支持搜索、快速预览、打开、复制、Pin、单项删除、按筛选结果批量删除、
   多选批量删除和清空确认。
4. 托盘、主页和全局快捷键对同一动作使用一致命名与状态反馈。

退出标准：多张 Pin 可独立操作且不阻塞主应用；Pin 初始图片显示尺寸与选区一致；
300 条历史记录下默认打开仍保持轻量。

### 阶段 4：主界面与视觉系统

1. 将主页调整为高频功能入口，设置项按界面、功能、热键、系统分组。
2. 统一颜色语义、排版层级、行高、间距、边框、圆角和焦点状态。
3. 用图标承载熟悉动作，文本用于明确命令；所有非直观图标提供 tooltip。
4. 完善加载、空状态、成功、失败、确认和不可用状态。
5. 验证最小窗口、100%/150%/200% 缩放和深浅主题。

退出标准：主要页面不存在文本截断、控件跳动、重叠或嵌套卡片；同类控件拥有一致
尺寸和反馈；键盘焦点清晰可见。

### 阶段 5：关键能力差距

按价值和风险依次评估并实现：

1. [x] 历史关键词搜索与筛选。
2. [x] PNG 之外的 JPG/WebP 导出，以及持久化默认导出格式。
3. [x] 默认目录和命名配置。
4. [x] 焦点窗口截图与更直接的窗口截图入口。
5. [x] 可配置的多动作全局快捷键。
6. [x] 自动滚动截图和高级 OCR/翻译失败恢复：滚动后自动捕获；OCR/翻译失败后可在原选区直接重试。

插件市场、AI 对话和在线协作不进入核心链路；只有在截图主流程、性能和稳定性完成
验收后再单独立项。

### 阶段 6：最终验收

自动验证：

- [x] 全量测试、严格 Clippy、格式检查和 git diff --check。
- [x] 100 次连续截图资源与延迟测试。
- [x] 4K 标注压力测试和性能报告。

2026-08-02 的单显示器 Release 记录显示：启动 p95 为 264.24ms（500ms 门槛），
快捷键到覆盖层 p95 为 96.38ms（100ms 门槛）；100 次连续截图与 4K 标注压力测试也通过。
详见 [Windows 手工验收记录](windows-manual-acceptance.md)，其中双屏和跨屏混合 DPI
矩阵已按当前用户范围暂缓，完整录屏 UI 手工矩阵仍待执行；FFmpeg 后端三种目标的机器
可读验收证据已补充。

Windows 实机矩阵：

- 单显示器 100%。
- 负坐标双显示器（当前暂缓）。
- 混合 DPI 双显示器（当前暂缓）。
- 最小设置窗口和窄选区。
- 多 Pin 的移动、缩放、复制、保存、透明度、关闭和主应用继续截图。
- FFmpeg 可用环境下的显示器、窗口、区域录屏及暂停/恢复/停止。

最终退出标准：逐项复核本计划的显式要求，所有 P0/P1 差距均有当前代码、自动测试或
实机记录证明完成；未完成的 P2 项必须有明确取舍，不能以“未发现问题”代替验收。

## 5. 提交与验证规则

- 每个有边界的修复或功能单独提交，提交信息沿用 Conventional Commits。
- 不提交验证失败或仅完成一半的功能。
- 先修正确性和交互，再做纯视觉调整。
- 新增交互优先抽取可测试逻辑，Windows 原生行为必须补充手工验收记录。
- 不因对齐 Snow Shot 引入 WebView 或让可选能力拖慢快捷键到覆盖层的主路径。
